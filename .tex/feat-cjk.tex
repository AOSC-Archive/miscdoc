% This feature controls language usage by altering the switches, and not by supplying the long text.
% Unless the text lines are short enough.


%%% Know issue: Cannot treat ASCII quotation marks in Markdown as non-CJK, causing using unexpected full-width glyph
\usepackage[PunctStyle=plain,RubberPunctSkip=false]{xeCJK}
\XeTeXlinebreaklocale "zh"
\XeTeXlinebreakskip = 0pt plus 1pt
\setCJKmainfont{Noto Sans CJK SC}[ItalicFont=Noto Serif CJK SC]
\setCJKromanfont{Noto Sans CJK SC}[ItalicFont=Noto Serif CJK SC]
\setCJKsansfont{Noto Sans CJK SC}[ItalicFont=Noto Serif CJK SC]
\setCJKmonofont{Noto Sans CJK SC}[ItalicFont=Noto Serif CJK SC]

% Redefine some styles
\titleformat*{\subsubsection}{\fontspec{Inter Semi Bold}\CJKfontspec{Noto Sans CJK SC Medium}\large}

% Redefine some commands
\renewcommand{\realrmfamily}[0]{\fontspec{XCharter}\CJKfontspec{Noto Serif CJK SC}}

% Text i18n
\renewcommand{\contentsname}{目录}
\renewcommand{\abstractsectionname}[0]{摘要}

\renewcommand{\TEXTpreauthor}[0]{作者：}
\renewcommand{\TEXTpredate}[0]{生成日期：}

\renewcommand{\finalizedocumentsectitle}[0]{后记}
\renewcommand{\findocnormaltext}[0]{\findocnormaltextZH}
